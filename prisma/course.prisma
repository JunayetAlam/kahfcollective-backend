model Course {
    id String @id @default(auto()) @map("_id") @db.ObjectId

    title       String
    description String
    tierLevel   TiersLevelEnum
    status      CourseStatus
    // contentId

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("courses")
}
